class AppConstants {
  // Duración de animaciones
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration animationDuration = Duration(milliseconds: 2000);
  static const Duration loginDelay = Duration(seconds: 2);
  
  // Tamaños
  static const double logoSize = 120.0;
  static const double iconSize = 60.0;
  static const double borderRadius = 12.0;
  static const double cardElevation = 2.0;
  
  // Credenciales de prueba
  static const String testEmail = 'admin@school.com';
  static const String testPassword = '123456';
}